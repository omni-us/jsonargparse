version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run:
          name: Build wheel package
          command: |
            sed -i -r '/^(coverage|pylint|mypy|sphinx|autodocsumm)/Id' requirements.txt
            pip install --user -r requirements.txt
            ./setup.py bdist_wheel
      - persist_to_workspace:
          root: .
          paths: ./dist/*.whl
  test-py37: &test-py37
    docker:
      - image: circleci/python:3.7
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Run unit tests
          command: |
            pip install --user $(ls ./dist/*.whl)"[all]"
            pip install --user python-subunit junitxml
            mkdir tests_report
            python -m subunit.run jsonargparse_test | $HOME/.local/bin/subunit2junitxml > tests_report/unittest.xml
      - store_test_results:
          path: ./tests_report
  test-py36:
    <<: *test-py37
    docker:
      - image: circleci/python:3.6
  test-py35:
    <<: *test-py37
    docker:
      - image: circleci/python:3.5
  publish-pypi:
    docker:
      - image: mauvilsa/docker-twine:1.11.0
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Publish Release on PyPI
          command: twine upload --username "${PYPI_USER}" --password "${PYPI_PASS}" ./dist/*.whl

workflows:
  version: 2
  build-test-publish:
    jobs:
      - build: &buildreq
          filters: &tagfilter
            tags:
              only: /^v\d+\.\d+\.\d+$/
      - test-py37: &testreq
          <<: *buildreq
          requires:
            - build
      - test-py36:
          <<: *testreq
      - test-py35:
          <<: *testreq
      - publish-pypi:
          filters:
            branches:
              ignore: /.*/
            <<: *tagfilter
          context: jsonargparse-context
          requires:
            - test-py37
            - test-py36
            - test-py35
